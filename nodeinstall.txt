=> apt-get update

=> apt-get install build-essential libssl-dev



=> curl https://raw.githubusercontent.com/creationix/nvm/v0.25.0/install.sh | bash

=> install curl if not

sudo apt update && sudo apt upgrade
sudo apt install curl

=> Install nvm

curl https://raw.githubusercontent.com/creationix/nvm/v0.36.0/install.sh | bash


=> close and reopen terminal

source ~/.profile

=> Install latest stable

nvm install 12.19.0






=> Install gulp-cli globally

npm install --global gulp-cli and 

gulp -v




=>From the <Magento_root> directory, copy and paste the contents of the following files:

package.json.sample to package.json
Gruntfile.js.sample to Gruntfile.js
grunt-config.json.sample into grunt-config.json


=> Install grunt-cli globally

npm install -g grunt-cli
npm install
npm update


=> Install local gulp for project
npm install --save-dev gulp

=> Install node-sass

npm install node-sass gulp-sass --save-dev


=> Install gulp-group-media-queries

npm install --save-dev gulp-group-css-media-queries

=> Install gulp-autoprefixer

npm install --save-dev gulp-autoprefixer

=> Install gulp-sourcemaps 

npm install gulp-sourcemaps --save-dev


=> Install gulp-watch

npm install --save-dev gulp-watch


=> Maximize the number of watchers for the ubuntu system

echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p





